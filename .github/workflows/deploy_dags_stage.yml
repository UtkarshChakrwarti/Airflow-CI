name: Deploy Placeholder

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run Placeholder Script
        run: |
          echo "Starting placeholder deployment..."

          # Airflow Stage Environment Details:
          # VM: pac-myntraairflowstage0
          #
          # Manual Login Steps:
          # 1. Connect to the jump host:
          #    ssh jumpuser@52.172.45.176  # Password: jumpuser
          #
          # 2. From the jump host, connect to the target VM:
          #    ssh wps@10.16.182.70
          #
          # Airflow Stage Web UI:
          # URL: stage-mws-airflow.myntra.com
          # Username: admin
          # Password: admin

          echo "Placeholder script executed. No actions performed."
